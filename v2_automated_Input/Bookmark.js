javascript:(async()=>{  const getText = (sel) => document.querySelector(sel)?.innerText.trim() || "";  const title = getText("h1");  const company = getText(%27li[class*="company"] span span span%27);  const location = getText(%27li[class*="location"] span span span%27);  const link = window.location.href;  await fetch("http://localhost:5000/job", {    method: "POST",    headers: {"Content-Type":"application/json"},    body: JSON.stringify({company, title, location, link})  });  alert("Job sent to Python!");})();